import { makeStyles } from "@mui/styles";
import { color, margin } from "../../style/globalVariables";

export const useSelectBox = makeStyles({
  useSelectBox: {
    minWidth: "120px",
    backgroundColor: color.white,
    marginBottom: margin.marginBottom50,
  },
});
